networks:
  a504-network:
    external: true

services:
  nextjs_app:
    container_name: nextjs_app
    image: nextjs_app:latest
    ports:
      - "3000:3000"
    networks:
      - a504-network
    restart: always
